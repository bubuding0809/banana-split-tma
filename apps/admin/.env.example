# Client-side vars only (exposed in the bundle).
# Server-side vars live in apps/admin/env/.env.development (local)
# or the Vercel project's environment variables (prod).

# Telegram bot username used by the Login Widget (no @ prefix)
VITE_TELEGRAM_BOT_USERNAME=""
